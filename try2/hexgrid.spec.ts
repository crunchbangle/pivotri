import { Point, rotate, orderTriangles, getNeighbouringTriangles, distanceBetweenPoints } from "./hexgrid";
import { describe, test, it, expect } from "@jest/globals";

// TODO:

// GridShape and HexGrid
describe("hexgrid shape constructions with sizes", () => {});

// rotate on HexGrid
describe("hexgrid rotations", () => {}); // 

// unit tests on the helper functions for rotation
describe("hexgrid functions", () => {});